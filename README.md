# Automatisation des Descriptions Personnalisées

## 🧩 Description du Projet
Ce projet a pour objectif d’automatiser la **génération de descriptions personnalisées** à partir d’un jeu de données structuré.  
Il illustre comment l’**intelligence artificielle** (IA générative) peut être utilisée pour produire des textes dynamiques et contextuels, notamment dans le secteur **logistique** ou **e-commerce**.

En combinant l’analyse de données avec la génération de langage naturel, ce notebook permet de créer automatiquement un **rapport PDF** regroupant des descriptions cohérentes et adaptées à chaque enregistrement du dataset.

---

## ⚙️ Fonctionnalités principales
- Chargement automatique d’un dataset contenant des informations sur les colis.  
- Analyse exploratoire des colonnes et de leurs types.  
- Génération de descriptions textuelles via l’API **OpenAI**.  
- Export automatique des résultats dans un fichier **PDF** formaté.  

---

## 🧠 Technologies utilisées
- **Python 3.10+**
- **Bibliothèques :**
  - `pandas` → manipulation et exploration de données
  - `openai` → génération de texte via API GPT
  - `fpdf` → création du rapport PDF
  - `time` → gestion des délais d’exécution entre les requêtes API

---


